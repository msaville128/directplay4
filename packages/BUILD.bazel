load("@bazel_skylib//:bzl_library.bzl", "bzl_library")
load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

sh_binary(
    name = "update",
    srcs = select({
        "@platforms//os:windows": ["update.ps1"],
        "//conditions:default": ["update.sh"],
    }),
    data = ["//packages:paket.dependencies"],
)

bzl_library(
    name = "extension",
    srcs = [
        "paket.main.bzl",
        "paket.main_extension.bzl",
    ],
)
